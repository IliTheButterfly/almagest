/**
 * Placeholder shell.
 *
 * Screens land with the PWA work; this exists so the build, the type checker
 * and the test runner have something real to chew on, and so the generated API
 * client is exercised end to end rather than sitting unused.
 */
export function App() {
  return (
    <main>
      <h1>Almagest</h1>
      <p>Backend is up. The interface is not built yet.</p>
    </main>
  );
}
